import ProviderDetailPage from '../components/ProviderDetailPage';

export default function MimecastPage() {
  return (
    <ProviderDetailPage
      slug="mimecast"
      providerName="Mimecast"
      statusPageUrl="https://status.mimecast.com"
    />
  );
}
